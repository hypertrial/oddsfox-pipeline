select
    h.market_id,
    h.outcome_index,
    h.clob_token_id,
    k.question,
    k.outcome_label,
    k.event_slug,
    k.market_slug,
    k.condition_id,
    k.sports_market_type,
    k.game_start_time,
    k.group_item_title,
    k.tags,
    k.clob_token_ids,
    k.yes_clob_token_id,
    k.no_clob_token_id,
    k.opposite_clob_token_id,
    k.is_active,
    k.is_closed,
    k.is_resolved,
    k.winning_outcome,
    k.winning_clob_token_id,
    k.market_volume_usd,
    k.stage_key,
    k.stage_rank,
    k.team_name,
    h.odds_hour_utc,
    h.odds_hour_epoch,
    h.open_price,
    h.high_price,
    h.low_price,
    h.close_price,
    h.avg_price,
    h.observed_points,
    h.first_timestamp,
    h.first_observed_at,
    h.last_timestamp,
    h.last_observed_at
from {{ ref('wc2026_token_hourly_odds') }} as h
inner join {{ ref('wc2026_knockout_market_tokens') }} as k
    on h.clob_token_id = k.clob_token_id
